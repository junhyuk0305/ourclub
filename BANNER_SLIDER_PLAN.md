# 배너 슬라이드 기획 (BANNER_SLIDER_PLAN)

> 페이지별 슬라이드 배너 도입 계획. **이 문서는 계획만** — 구현은 승인 후 진행.
> 작성일 2026-06-24 · 브랜치 `feat6`

## 0. 결정 사항 (확정)

| 항목 | 결정 | 의미 |
|---|---|---|
| **관리 모델** | 정적 시작 → DB 확장 | 공통 `BannerSlider` 컴포넌트 + 페이지별 정적 배열로 1차 출시. **동일 props 인터페이스**로 후에 `banners` 테이블 백엔드 교체. 컴포넌트는 데이터 출처를 모름. |
| **광고 범위** | 지금은 자사 홍보/공지만, **추후 외부 광고** | 1차엔 내부 콘텐츠만. 단, 스폰서 표기(`광고` 라벨)·노출/클릭 집계 자리를 처음부터 스키마/타입에 예약. |

---

## 1. 목표

5개 핵심 화면에 **자동 순환 슬라이드 배너**를 넣어, 각 화면의 주 사용자에게 맞는 ① 서비스 이용 안내 ② 자사 홍보/공지(추후 광고)를 노출한다.

> **전역 원칙 — 최소 침습 배치**: 어떤 배너도 기존 콘텐츠(히어로 메시지, 핵심 액션, 탭/리스트)를 가리거나 밀어내지 않는다. 배너는 **각 페이지 콘텐츠 맨 아래(푸터 직전)** 에 슬림하게 둔다(상단 X). 시각적 위계는 페이지 핵심 콘텐츠보다 낮게.

> **v2 디자인 메모(피드백 반영)**: ① 무거운 브루탈리스트(하드섀도·도트패턴·큰 아이콘박스) 제거 → 슬림 1행(아이콘 칩 + 제목/부제 + CTA). ② 높이 대폭 축소(112/128px). ③ 상단→하단 이동. ④ 자동 넘김은 reduced-motion 환경에서도 동작(전환만 즉시), hover/focus 정지.

| # | 페이지 | 주 사용자 | 파일 |
|---|---|---|---|
| 1 | 동아리 찾기 | 동아리 찾는 학생 | `src/pages/public/ClubRecruit.tsx` (또는 동아리 목록 화면) |
| 2 | 홈페이지 | 신규/비로그인 방문자 | `src/pages/public/Home.tsx` |
| 3 | 기업 라운지 | 기업 담당자 | `src/pages/public/B2BLounge.tsx` |
| 4 | 학생 마이페이지 | 로그인 부원 | `src/pages/user/MyPage.tsx` |
| 5 | 운영진 워크스페이스 | 동아리 운영진 | ⚠️ 아래 §3 배치 결정 참고 |

---

## 2. 아키텍처 — 공통 컴포넌트 1개

```
src/components/ui/BannerSlider.tsx   ← 신규. 데이터 출처 무관(presentational)
src/data/banners.ts                  ← 신규. 페이지별 정적 슬라이드 배열 (1차 데이터 소스)
                                        후에 useBanners(page) 훅으로 교체 → banners 테이블
```

**원칙**
- 기존 `HeroSliderBlock`(`blockKit.tsx`)은 웹빌더 전용 → **재사용하지 않고** 별도 경량 컴포넌트 신설. (웹빌더 블록 트리/편집모드 의존성을 공개 페이지로 끌고 오지 않음.)
- 애니메이션은 **CSS transition + 기존 디자인 토큰만** 사용. 새 캐러셀 라이브러리(embla/swiper) 도입 안 함 (CLAUDE.md §2 단순성).
- 페이지는 `<BannerSlider slides={...} page="home" />` 한 줄만 추가. 셸/레이아웃 규칙(§6) 위반 없음.

**`Slide` 타입 (1차 + 광고 확장 예약)**
```ts
type Slide = {
  id: string;
  title: string;          // ≤ 24자
  subtitle?: string;      // ≤ 40자
  imageUrl?: string;      // 없으면 색상 배경 + 텍스트
  bg?: string;            // tailwind 색 클래스 (이미지 없을 때)
  cta?: { label: string; href: string };  // label ≤ 10자
  // ↓ 광고 확장용 — 1차엔 미사용, 스키마만 예약
  sponsored?: boolean;    // true면 '광고' 배지 표기
  startAt?: string;       // 노출 시작 (DB 단계)
  endAt?: string;         // 노출 종료
};
```

**컴포넌트 동작**
- 자동 순환 5초, hover/focus 시 일시정지, 좌우 화살표 + 하단 dot.
- 모바일 스와이프(터치) 지원, 키보드 ←/→ 이동.
- `prefers-reduced-motion`이면 자동재생 OFF (정지 + 수동 조작만).
- 슬라이드 0~1개면 화살표/dot 숨김, 0개면 컴포넌트 자체 렌더 안 함.
- `sponsored: true` → 좌상단 `광고` 배지(추후).
- (DB 단계) 노출/클릭 시 집계 이벤트 hook 자리 예약.

---

## 3. 페이지별 배치 + 콘텐츠 방향

각 페이지 주 사용자에 맞춘 슬라이드 예시(자사 홍보/공지). 실제 카피·이미지는 구현 시 확정.

### 1) 동아리 찾기 — 학생
- **배치**: 페이지 맨 아래(후기 섹션 아래).
- **슬라이드**: ① 동아리 가입하는 법(이용안내) ② 마감 임박 모집 큐레이션 ③ 프로필 완성하면 좋은 점 ④ 오렌지 뱃지(검증 동아리)란?

### 2) 홈페이지 — 신규 방문자
- **배치**: 페이지 맨 아래(Gateway 아래) 풀블리드 띠.
- **슬라이드**: ① 서비스 3단계 이용법 ② 핵심 기능 소개 ③ 가입 유도 CTA ④ "기업이세요? 라운지로" 분기.

### 3) 기업 라운지 — 기업 담당자
- **배치**: 프로젝트 리스트 섹션 아래(페이지 하단).
- **슬라이드**: ① 프로젝트 의뢰하는 법 ② B2B 계약·안전 가이드 ③ 수수료/정산 안내 ④ 협업 성사 사례.
- ⚠️ **카피 제약**: 채용/구인/구직/인력/소개/알선 금지 → 프로젝트/도급/협업 (`feedback_b2b_terminology`, 직업안정법 방어).

### 4) 학생 마이페이지 — 로그인 부원
- **배치**: 탭 그리드 아래(컨테이너 하단).
- **슬라이드**: ① 출석/기능 사용법 ② 신규 기능 공지 ③ 내 동아리 이벤트 ④ 프로필/탈퇴 등 안내.

### 5) 운영진 워크스페이스 — 운영진  ✅ 배치 확정
- `Workspace.tsx`는 **풀스크린 1-Page 웹빌더**(CLAUDE.md §6 레이아웃 예외) → 빌더 캔버스에는 배너를 넣지 않는다(편집 UX 충돌).
- **확정**: 운영진 영역에서는 **운영진 랜딩 = 어드민 대시보드(`DashboardAdmin.tsx`) 콘텐츠 최상단 한 곳에만** 배너를 둔다. 다른 어드민 하위 페이지에는 배너를 넣지 않는다(최소 침습).
- 슬라이드: ① 기능 가이드(출석/모집/웹빌더) ② 운영 베스트프랙티스 ③ 신규 도구 공지 ④ B2B 프로젝트 기회.

---

## 3-A. 제작 방식 — 왜 "구조화 컴포넌트"인가 (업계 표준 비교)

실제 플랫폼들의 배너 구현은 4가지로 나뉜다.

| 방식 | 설명 | 사용처 | 본 프로젝트 채택 |
|---|---|---|---|
| ① **구조화 컴포넌트** | 이미지/배경색 + 제목·부제·CTA를 **필드로** 저장, 앱이 렌더 | 토스·배민·쿠팡·당근·Shopify Slideshow | ✅ **1차 채택** |
| ② 정적 이미지 + 링크 | 완성 이미지 통짜 + 클릭 URL (글자가 이미지에 박힘) | 네이버 메인 배너 | ❌ 반응형·접근성·유지보수 손해 |
| ③ HTML5 리치미디어 | HTML/CSS/JS 소재를 iframe 격리 삽입 | Google Display Network | 2차 외부광고 시 **iframe 격리로만** |
| ④ 애드서버/프로그래매틱 | 광고태그 한 줄 → GAM이 노출·집계·타게팅 | 외부광고 수익 매체 | 2차 후반(규모 확대) 옵션 |

- **HTML 직접 삽입(③)은 자사 페이지에 직접 박지 않는다** — XSS·레이아웃 붕괴·성능·디자인 일관성 위험. 외부 광고가 들어올 때만 iframe으로 격리.
- **통짜 이미지+링크(②)도 지양** — 대신 *배경 이미지 + 구조화 텍스트/CTA*. 색상배경+텍스트만으로도 1차 출시 가능 → 이미지 제작 병목 제거, 카피 수정 시 재제작 불필요, 모바일 글자 깨짐 없음.
- 참고 표준: IAB 사이즈(빌보드 970×250 / 리더보드 728×90 / 모바일 320×50·320×100), 디스플레이 용량 권장 ≤150KB. → **외부 광고 소재를 받을 때 이 규격을 요구**하면 호환.

## 4. 배너 규격 (디자인 토큰: 브루탈리스트 — 검정 1px 테두리, 오렌지 포인트, 샤프 코너, hard-shadow)

| 항목 | 규격 |
|---|---|
| **슬롯 크기** | 콘텐츠 폭 100%. 높이 **데스크탑 128px / 모바일 112px** (슬림 1행, v2 축소) |
| **이미지 원본** | 2400×600 권장(2x), 안전영역 중앙 정렬. 텍스트는 좌측 30~50%에 오버레이 |
| **포맷/용량** | webp 우선(jpg 폴백), **슬라이드당 ≤ 300KB** |
| **텍스트 한도** | 제목 ≤ 24자 · 부제 ≤ 40자 · CTA 라벨 ≤ 10자 |
| **슬라이드 수** | 페이지당 **3~5개** (최대 5, 그 이상 노출 금지) |
| **자동재생** | 5초 간격, hover/focus 정지, `prefers-reduced-motion` 시 OFF |
| **전환** | fade 또는 slide, 0.4~0.6s (기존 토큰 재사용) |
| **색/폰트** | 오렌지 `#f97316` 포인트, 흑/백/그레이, `font-black/bold`, border-radius 없음 |
| **접근성** | 슬라이드 `aria-label`, 화살표 button, 키보드 ←/→, 대비 AA |

---

## 5. 제한·정책 (제한 내용)

**1차 (자사 콘텐츠)**
- 콘텐츠 출처: 내부 홍보/공지/이용안내만. 외부 링크는 자사 도메인/검증 동아리·기업으로 한정.
- 페이지당 활성 슬라이드 **최대 5**. 과다 노출 금지.
- 허위·과장 표현 금지. B2B 면은 §3-3 용어 제약 준수.
- 모든 슬라이드 CTA는 실제 도달 가능한 라우트로 한정(깨진 링크 금지).

**2차 (외부 광고 도입 시 — 미리 설계만)**
- 외부/유료 슬라이드는 `sponsored: true` → **`광고` 배지 의무 표기**.
- 노출수·클릭수 집계(이벤트 hook → 추후 테이블/지표).
- 금지 업종·심의 정책 별도 수립(도박/대출/성인/허위과장 등 차단).
- 노출 기간(`startAt`/`endAt`)·페이지 타게팅·정렬 우선순위 운영자 관리.
- 광고:자사 콘텐츠 노출 비율 상한 등 운영 정책.

---

## 6. (확장) DB 스키마 초안 — 2차에서 적용

```sql
create table banners (
  id uuid primary key default gen_random_uuid(),
  page text not null check (page in ('home','recruit','lounge','mypage','workspace')),
  title text not null,
  subtitle text,
  image_url text,
  bg text,
  cta_label text,
  cta_href text,
  sponsored boolean not null default false,
  sort int not null default 0,
  active boolean not null default true,
  start_at timestamptz,
  end_at timestamptz,
  created_at timestamptz default now()
);
-- RLS: 읽기 public(active=true & 노출기간 내), 쓰기 master만
-- (2차) banner_impressions / banner_clicks 집계 테이블
```
정적 `src/data/banners.ts` → `useBanners(page)` 훅으로 교체. **`BannerSlider`는 변경 없음**(props 동일).

---

## 7. 구현 단계

1. ✅ `src/components/ui/BannerSlider.tsx` — 5개 layout 템플릿(`feature`/`stat`/`steps`/`promo`/`image`) + 3개 테마(light/dark/orange), 자동재생 5s·hover/focus 정지·`prefers-reduced-motion` OFF·키보드·스와이프·dot/화살표·`sponsored` 배지.
2. ✅ `src/data/banners.ts` — 5개 페이지 슬라이드(각 3장), `getBanners(page)`. 카피 한도·확정 라우트 CTA 준수.
3. ✅ 5개 페이지 **하단** 삽입(최소 침습): Home(Gateway 아래 full-bleed 섹션), B2BLounge(프로젝트 리스트 아래 — `loungeSlides`로 누적 완료 수치 실집계 주입), ClubRecruit(후기 섹션 아래), MyPage(탭 그리드 아래), DashboardAdmin(콘텐츠 하단).
4. ✅ 운영진 = DashboardAdmin 한 곳만(§3-5).
5. ⬜ `lint`(=tsc) 통과 확인 완료. **브라우저 시각 QA 미완** / 커밋 미완.
6. ⬜ (2차, 별도) `banners` 테이블 + `useBanners` 훅 + 마스터 콘솔 CRUD + 노출/클릭 집계.

**구현 파일**: `src/components/ui/BannerSlider.tsx`(신규), `src/data/banners.ts`(신규), + 5개 페이지(import 2줄 + 삽입).

---

## 8. 미결 사항 (구현 전 확인)

- [x] ~~운영진 배너 위치~~ → **확정**: 어드민 대시보드(`DashboardAdmin.tsx`) 최상단 한 곳만 (§3-5).
- [x] ~~제작 방식~~ → **확정**: 구조화 컴포넌트(③ HTML 직접삽입 ❌, ② 통짜이미지 ❌) (§3-A).
- [ ] 1차 슬라이드를 **색상배경+텍스트만**으로 낼지, **배경 이미지까지** 제작할지(이미지 제작 주체).
- [ ] 홈/라운지 히어로와 배너의 **시각적 위계** 디테일(히어로 압도 방지 — 전역 원칙 §1).
