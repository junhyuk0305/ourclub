# OURCLUB 온보딩 UX/데이터 통합 기획안

로그인/회원가입 → 마이페이지/운영진 워크스페이스까지의 사용자 경험을 역할(학생·부원·운영진)별로
통일하고, 회원 데이터 수집·동아리 가입 프로세스의 공백을 메우기 위한 기획·실행 기록.

---

## 0. 핵심 진단 — "가입" 경로가 3개로 쪼개져 있음

| 경로 | 테이블 | 의미 | 결과 | 승인자 |
|---|---|---|---|---|
| A. 새 동아리 등록 | `club_registration_requests` | 내 동아리를 플랫폼에 **창설** | `clubs` 생성 + 나를 `role='운영진'` | 마스터 |
| B. 기존 동아리 운영진 합류 | `club_join_requests` (`role_title`) | 기존 동아리의 **운영진**으로 합류 | `club_members role='운영진'` | 마스터 **그리고** 동아리 운영진 (이중) |
| C. 모집 지원 → 부원 | `recruitment_applications` | 모집 공고에 **지원** | 최종합격 시 트리거가 자동 `role='부원'` 추가 | 동아리 운영진 |

- **공백**: 일반 학생이 "이 동아리에 **부원으로** 들어가고 싶다"는 셀프 가입 경로가 없음 (C 모집 지원 또는 운영진 초대뿐).
- `AdminRoute`는 비운영진을 전부 `/club-setup`으로 보내는데, `/club-setup`은 A·B(운영진용)만 보여줘 부원 지망생이 막힘.
- B의 승인자가 마스터와 동아리 운영진으로 **이중화**되어 정합성 위험.

---

## 1. 확정된 결정 사항

1. **회원가입 데이터**: 프로필 완성을 **필수 게이트**로. 필수 5필드 = 대학교·전공·학적상태·전화번호·생년월일.
2. **프로필 게이트 적용 대상**: 학생·운영진만. 기업 담당자(corp)·마스터는 면제.
3. **로그인 진입 화면**: 로그인 폼 우선 + 하단 회원가입 전환 (역할 선택 화면 제거).
4. **계정 타입**: 단일 계정 통합(학생/운영진 구분 가입 폐지). 동아리 만들기는 로그인 후 액션.

---

## 2. 구현 완료 항목

### (1) 로그인/회원가입 흐름 통일
- `src/pages/public/Onboarding.tsx` — 역할 선택 3카드 제거, **로그인 폼 우선** + 하단 "회원가입" 전환(단일 form/모드 토글). 기업은 하단 보조 링크.
- 가입 시 이메일 인증 필요(`needsConfirmation`)하면 "이메일을 확인하세요" 안내 화면.

### (2) 로그인 후 원래 위치 복귀
- `src/App.tsx` — 보호 라우트가 미로그인 시 `state={{ from: location }}` 전달.
- `Onboarding.tsx` — 로그인/가입/Google 성공 후 `from`으로 복귀(없으면 `/mypage`).
- `src/contexts/AuthContext.tsx` — `signUp`이 `{ error, needsConfirmation }` 반환.

### (3) 프로필 완성 필수 게이트
- `AuthContext.tsx` — `isProfileComplete` 파생값(5필드 모두 존재 시 true).
- `src/pages/public/ProfileSetup.tsx` (신규) — 필수 5필드 폼. 제출 → `profiles` update → `refreshProfile` → `from`/`/mypage`. 완성자는 진입 차단.
- `App.tsx` — `/profile-setup` 라우트(`RequireAuth`=세션만). `ProtectedRoute`·`AdminRoute`에 완성도 게이트(미완성 → `/profile-setup`). **기업·마스터 면제.**

### (4) /club-setup 의도 기반 분기 (부원 길 열기)
- `src/pages/public/ClubSetup.tsx` — 선택 화면을 **부원 / 운영진**으로 재구성.
  - 상단 강조 카드: "동아리에 가입하고 싶어요" → `/clubs` (모집 둘러보기 → 지원 → 자동 부원)
  - 하단 운영진 영역: 기존 동아리 합류 / 새 동아리 등록.

### (5) 역할별 착지 유도
- `src/pages/user/MyPage.tsx` — `RoleNudgeBanner` 추가. `isAdmin`이면 운영진 워크스페이스, `isCorpUser`면 기업 센터로 전환 배너. (자동 이동 대신 유도 — 학생·운영진 겸직 고려)
- 빈 지원내역 CTA(동아리 탐색)는 기존에 존재.

### (6) 동아리 등록/운영 진입점 추가 (학생의 dead-end 해소)
- 기존 문제: 일반 학생이 UI 클릭만으로 `/club-setup`(등록·합류 허브)에 도달 불가 (AdminRoute 리다이렉트로만 도달).
- `src/components/layout/Header.tsx` — 로그인·비운영진·비기업 사용자에게 **"동아리 운영하기"** 링크(데스크톱+모바일) → `/club-setup`.
- `src/pages/user/MyPage.tsx` — `RoleNudgeBanner`의 일반 학생 케이스에 "동아리를 운영하시나요? 등록·합류 →" 보조 카드.

### (7) 구성원(부원·운영진) 추가 — 라벨/문구 명확화
- 발견: 부원·운영진 추가 기능은 `MembersAdmin > InviteModal`에 **이미 존재**(이메일 초대/계정없이 추가 + 역할 드롭다운 부원·운영진). 운영진 추가 시 `club_members role='운영진'`이라 워크스페이스 권한 자동 부여.
- 정리: 모달/버튼 라벨 "부원 추가" → **"구성원 추가"**, 안내·에러 문구 일반화, 운영진 선택 시 권한 안내 추가.
- 미해결: 이메일 초대는 *이미 가입한* 계정만 검색 가능 (미가입자 사전초대 불가) → 후속.

---

## 3. 역할별 여정 (End-to-End)

**학생/부원** — 허브: MyPage
```
가입 → 프로필 완성(필수) → /clubs 탐색 → 모집 지원(프로필 자동완성)
 → 합격 시 자동 부원 → MyPage 활동/출결·출석체크·Pulse 활성
```

**운영진** — 허브: DashboardAdmin + Workspace
```
가입 → 프로필 완성(필수) → /club-setup(운영진) → A(새 동아리)/B(기존 합류)
 → 승인 → isAdmin=true → 워크스페이스 (대기 중엔 ClubSetup 상태 화면)
```

---

## 4. 남은 결정/후속 과제 (미착수)

- **부원 셀프 가입 방식 (택1)**
  - 4-A (현재 적용): 부원 진입 = 모집 지원으로 단일화 (스키마 변경 없음).
  - 4-B: `club_join_requests`에 `role` 추가해 부원 신청 수용.
  - 4-C: 신규 `club_member_requests` 테이블 분리.
- **(B) 운영진 합류 승인 일원화**: 마스터 권한 축소, 동아리 운영진으로 단일화 검토.
- **이메일 초대 ↔ 가입 연결**: 운영진이 이메일로 초대했지만 미가입자인 경우, 가입 시 자동 연결(클레임) 처리.
- **지원 상태 알림**: MyPage 알림 탭 "준비중" → 실제 상태 변화 알림.
