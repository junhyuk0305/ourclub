# B2B 핸드오프 — 남은 일 / 블로커 (별도 기록)

> 작성: 2026-06-24. 이 세션과 **별도 프로세스가 B2B 전 영역을 동시 작업**하던 중,
> 충돌 없이 할 수 있는 검증·정리만 수행하고 **하지 못한 내용을 여기 따로 남김**.
> 동시 편집 충돌을 피하기 위해 B2B 코드·마이그레이션·플랜·법률문서·SCHEMA.md는 **건드리지 않았다.**

## ✅ 검증 완료 (이 세션)
- `tsc --noEmit` (= `npm run lint`) **통과(EXIT 0)** — 결합 상태 정상.
- 내 M1 진입점 잔존 확인: `B2BHandoffModal` 가 `B2BAdmin.tsx`·`CorpDashboard.tsx` 양쪽에 연결됨.
- R4 미납 게이트 연결 확인: `B2BProposalAdmin.tsx`가 `b2b_club_has_overdue` RPC 호출.
- 내가 잘못 만든 중복 마이그레이션 `20260624040000_b2b_settlements.sql` **삭제함**(정본 `20260624050000_b2b_settlement_r4.sql`과 테이블·정책명 충돌이라).
- 마이그레이션 정책명 전수 검사: B2B 정책명 **충돌 없음**(전부 고유).

## 🟥 블로커 — `db push` 전에 반드시 해결
**마이그레이션 버전(타임스탬프 prefix) 중복: `20260624060000` 2개**
- `20260624060000_b2b_application_consent.sql` (동의 컬럼)
- `20260624060000_b2b_reviews_m3.sql` (양방향 리뷰)
- Supabase는 이 14자리를 migration version(=PK)으로 식별 → 같은 버전 2개면 `db push` 시 한쪽이 충돌/스킵되어 **스키마 드리프트** 발생.
- 권장 수정: `b2b_application_consent.sql`을 빈 슬롯으로 renumber (예: `20260624080000_b2b_application_consent.sql`). 컬럼 추가뿐이라 순서 의존성 없음.
  - ⚠️ `b2b_reviews_m3` 쪽은 **건드리지 말 것** — `B2B_HANDOFF_FLOW_PLAN.md`가 그 파일명을 명시 참조함.
- ※ 이건 동시 프로세스 소유 파일이라 충돌 방지를 위해 **내가 직접 고치지 않음**. 담당 쪽에서 처리 요망.

## 🟧 아직 안 된 일
- **마이그레이션 원격 적용**: 사용자가 "마이그레이션 진행함"이라 한 시점 이후 `040000~070000`(정산·리뷰·마일스톤·동의)이 추가됨 → **이들은 아직 미적용일 가능성**. 위 중복 버전 해결 후 `supabase db push` 재실행 필요.
- **브라우저 QA**: 매칭완료→PL지정→계약→킥오프→마일스톤/산출물→정산→리뷰 end-to-end 미검증. 특히 비공개 버킷(`b2b-contracts`/`b2b-deliverables`) 서명URL 열람, 미납 트리거가 실제 신규 제안을 막는지.
- **커밋**: 사용자 요청대로 보류(나중에).

## ⚪ 의도적 보류 (플랜에 명시)
- R4 인증뱃지 자동정지 — 보류(검증 상태 데이터 훼손 우려). 신규 제안 잠금으로 핵심 제재는 충족.
- M2/M3 외 후속(신뢰점수·정산이력 표시 등) — 후속 과제.

## 참고: 동시 프로세스가 완료한 범위 (내가 만든 게 아님)
- M2 마일스톤/산출물(`20260624070000` + `b2b-deliverables` 버킷), M3 정산·미납제재(`050000`)·양방향 리뷰(`060000`), 동의 컬럼 2종, 법률문서 확정본, `B2BHandoffModal` 정산/마일스톤/리뷰 섹션, `SCHEMA.md` 핸드오프 테이블 문서화.
