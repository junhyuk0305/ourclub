# 채용 페이지 재설계 — 스펙

> 동아리 채용 페이지(`/clubs/:id/recruit`, `ClubRecruit.tsx`)를 **"동아리 인스타그램 프로필"** 모델로 재설계한다.
> 결정 일자: 2026-06-04 / 브랜치: feat4 / 상태: 설계 확정, 구현 대기

---

## 1. 배경 — 지금 무엇이 문제인가

동아리마다 공개 페이지가 이미 **2개**로 갈려 있다:

| | 소개 1-page | 채용 page |
|---|---|---|
| 라우트 | `/clubs/:id` → `ClubDetail` | `/clubs/:id/recruit` → `ClubRecruit` |
| 에디터 | Workspace 웹빌더 | `/admin/recruit-page` → `RecruitPageBuilder` |
| 데이터 | `club_pages.draft/blocks` (블록 JSON, 발행 모델) | `clubs.recruit_page` (섹션 토글 설정) |
| 성격 | 동아리마다 완전 유니크, 에이전시급 | 정해진 5섹션 토글 |

**문제:** 현재 채용 페이지의 hero·story·faq 섹션이 소개 1-page와 정면으로 겹친다. 소개가 이미 브랜드·스토리텔링을 풍부하게 하는데 채용이 축소판으로 또 한다 → 운영진 이중 편집·드리프트, 채용 페이지 정체성 모호("소개 축소판 + 공고 몇 개"). 게다가 `.limit(3)` + 진행중만 필터라 비수기엔 거의 빈 페이지.

## 2. 핵심 컨셉

> **브랜드 표현은 소개 페이지가, 전환·신뢰는 채용 페이지가 담당.**
> 채용 페이지 = 동아리 인스타그램 프로필. **모든 동아리 동일 레이아웃, 내용(게시물·공고)만 다르다.**

웹빌더(전부 유니크)와 **정반대 철학**으로 두 페이지의 역할을 분리. 지원자가 여러 동아리를 비교할 때 "어디에 뭐가 있는지" 다시 학습할 필요가 없어야 신뢰가 생긴다(잡코리아·링크드인이 공고를 표준화하는 이유와 동일).

## 3. 레이아웃 (위 → 아래)

### 3.1 프로필 헤더 (기존 검정 헤더바를 흡수·통합)
- 뒤로가기 · 아바타(로고) · 동아리명 · 유형 · 인증 뱃지 · **소개 보기** 링크
- 카운트 라인: `모집중 N · 후기 N · ★평점`
  - 후기 0개 → 평점 숨김
  - 모집중 0개 → "모집 준비중" 표기
- **태그라인 한 줄** (운영진의 유일한 커스텀 항목). 비어 있으면 `clubs.one_line_desc` 폴백
- **관심 등록** 버튼 — 기존 `bookmarks` 테이블 재사용. "다음 모집 알림받기"의 기반

### 3.2 모집중 공고 = 지원 배너 (피드 맨 위 고정)
- 게시물 그리드와 **뚜렷이 구분되는 가로형 배너**로 표시 (전환 목표 명확화)
- 내용: 제목 · D-day · 지원 버튼
- 여러 개면 **마감 임박순** 정렬
- 클릭 → 기존 **JD 모달** → 지원 (`/clubs/:id/apply?rid=...`)
- 마감된 공고는 그 아래에 **흐리게 히스토리**로 유지 (열람 가능, 지원 버튼 비활성)
  - → 인스타의 "지난 게시물"처럼 신뢰 신호. 비수기 빈 페이지 문제 해결

### 3.3 게시물 피드 = 3열 정사각 그리드 (인스타 프로필형)
- 데이터: `posts` 중 `is_published = true` **전체 공개 게시물**
- 이미지 없는 글 게시물 → 색/텍스트 타일로 렌더
- 클릭 → 기존 `/stories/:id` (`StoryDetail`)

### 3.4 후기 (피드 하단)
- 잡플래닛식 평점·작성 (현행 `club_reviews` 유지)
- 후기 적은 초기 동아리도 빈 탭 없이 자연스럽게 보이도록 **탭이 아니라 하단 섹션**

## 4. 스타일 · 동선

- **모노크롬** (흑/백/회). `brand_color` 사용 전면 제거. 색은 아바타 정도에만 흔적
- 동아리 목록/검색 진입은 **항상 소개로**. 채용은 소개의 **플로팅 '지원' 버튼**(`config.showFloatingBtn`)으로만 진입 → "소개 = 정문, 채용 = 행동 레이어"
- 게시물 0개인 신규 동아리 → 핀 공고 + 후기로 페이지 구성, 그리드 자리엔 "아직 게시물 없음" 안내

## 5. 구현 범위

### 5.1 공개 페이지 — `src/pages/public/ClubRecruit.tsx` (재구성)
- 제거: `HeroSection`, `StorySection`, `FAQSection`, 모든 `brand_color` 분기, 상단 검정 헤더바
- 추가/변경:
  - 통합 **프로필 헤더** (카운트·태그라인·소개보기·관심등록)
  - **지원 배너**(진행중 공고, 마감임박순) + 마감 히스토리
  - **3열 게시물 그리드** (`posts` 전체, 이미지/텍스트 타일)
  - **후기 섹션** 하단 유지 (작성 자격만 강화 — 5.2 참조)
- 데이터 로드: 공고 `.limit(3)` 해제(진행중 전체 + 마감 일부), `posts` 전체 공개분, `club_reviews` 유지

### 5.2 DB — 마이그레이션 1건 (신규)
- **후기 작성 자격을 "부원/합격자만"으로 강화**: `club_reviews` INSERT RLS 조건에
  `club_members`(해당 club 활동중) **또는** `recruitment_applications`(해당 club 지원 이력) 존재 검사 추가
- *코드만으로는 불가 — DB 변경 필수.* 신규 마이그레이션은 **대시보드 SQL 수동 적용**(CLI 마이그 이력 비어있음 → push 금지, 온보딩 메모 관례 준수)

### 5.3 에디터 — `RecruitPageBuilder` 축소
- 섹션 토글 빌더(hero/recruitments/story/reviews/faq) 제거
- **설정 어드민의 태그라인 입력칸 하나**로 축소
- `clubs.recruit_page` JSONB는 유지하되 **tagline 필드만 읽음** (기존 섹션 설정 데이터는 무시; 스키마 변경 없음)
- → 이중 편집·유지보수 부담 최소화 (부수 효과: 에디터도 단순해짐)

## 6. 미결 (구현 단계에서 결정)

- "관심 등록" 버튼 위치: 헤더 vs 비수기 상단 배너
- 정사각 그리드에서 이미지 없는 글 게시물 타일의 색 규칙
- 새 모집 공고 발행 시 관심 등록자에게 **인앱 알림 트리거 신설** 여부 (후속, `notifications` 시스템 재사용 가능)
- `RecruitPageBuilder` 라우트(`/admin/recruit-page`)를 설정 페이지로 합칠지, 축소 페이지로 남길지

## 7. 결정 로그 (왜 이렇게 정했나)

| 항목 | 결정 | 이유 |
|---|---|---|
| 2페이지 유지 | 유지하되 역할 분리 | 소개=브랜드(유니크), 채용=신뢰/전환(표준) |
| 브랜드 컬러 | 제거(모노크롬) | 브랜드는 소개가 전담, 채용은 심플·표준 |
| 비수기 화면 | 마감 공고 히스토리 유지 | 인스타 "지난 게시물"처럼 빈 페이지 방지 + 신뢰 |
| 커스텀 범위 | 태그라인 한 줄만 | 표준화 유지, 에디터 단순화 |
| 피드 내용 | 전체 공개 게시물 + 공고 핀 | 사용자 의도("인스타 게시물 피드")에 충실 |
| 피드 형태 | 3열 정사각 그리드 | 인스타 프로필형, 스캔 빠름 |
| 후기 위치 | 피드 하단 | 초기 동아리 빈 탭 방지 |
| 후기 자격 | 부원/합격자만 | 스팸·허위 차단, 신뢰도 |
| 목록 진입 | 항상 소개로 | "소개=정문" 일관 규칙 |
| 소개→채용 | 플로팅 '지원' 버튼 | 기존 `showFloatingBtn` 재사용 |

---

**관련 코드:** `src/pages/public/ClubRecruit.tsx`, `src/pages/admin/RecruitPageBuilder.tsx`, `src/pages/public/ClubDetail.tsx`(소개·플로팅버튼), `src/pages/user/MyPage.tsx`(bookmarks), `supabase/migrations/20260517000000_recruit_page_builder.sql`(club_reviews·recruit_page)
