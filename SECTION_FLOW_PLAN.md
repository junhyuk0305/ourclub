# SECTION_FLOW_PLAN — 공개 영역 섹션 흐름(IA) 재기획

> **목적**: "AB · 따뜻한 세련" 디자인 토큰 반영(Phase A 완료)에 맞춰, 재정의된 브랜드(운영=주인공 / 자생=확장, 가치사다리 ①홈페이지 ②운영 ③자생)를 기준으로 공개 4페이지의 **섹션 구성·순서·카피 훅·CTA**를 다시 설계한다.
> **정렬 기준**: 브랜드 §1 가치사다리, §6 카피 정본, §7 용어규칙(금지어). 비주얼은 DESIGN_SYSTEM.md / DESIGN_MASTER.html.
> **상태**: Phase B 산출물 — **승인 대기**. 승인 후 Phase C(페이지별 구현) 진행.

---

## 0. 공통 원칙 (4페이지 전부 적용)

1. **가치사다리 순서로 말한다**: 홈페이지(가장 구체적 갈고리) → 운영 → 자생. 자생(기업 프로젝트)은 항상 마지막 스텝으로 배치, 전면에 올리지 않는다.
2. **주인공은 운영진**. 2차 학생(둘러보기), 3차 기업(라운지)은 보조 게이트웨이로.
3. **카피는 §6 정본 그대로**. "무료" 헤드라인 금지(본문에서 결과로 체감). "B2B"·"수주"·"채용/구인/구직/인력/소개/알선" 공개 카피 제거.
4. **검증/오렌지 뱃지는 보조 신뢰 마크**로만 언급(핵심 해자 표현 금지). "안전 검증 완료/100% 검증" 류 폐기.
5. **비주얼**: 모든 박스=`bg-white border border-sand-200 rounded-card shadow-soft`, 클릭형 카드 hover=`shadow-soft-lg -translate-y-1`. 검정 테두리·하드섀도·`bg-orange-500`·각짐 전면 제거. 주 CTA=`btn-grad text-white shadow-btn`. 상태색=`statusColor` 토큰.

### 공유 컴포넌트 (Phase C 충돌 방지 — Home에서 확정, 나머지가 참조)
- **`SectionHead`** (Home 내 정의 → 그대로 유지, 토큰만 교체): eyebrow + H2 + sub + "전체보기" 링크.
- **`FilterChips`** (Home 내 정의 → 토큰 교체): 활성=`bg-ink text-white`, 비활성=`bg-white border-sand-300 hover:bg-sand-50`. (DESIGN_MASTER 게시판 칩과 동일)
- **`ClubCard`** (목록 카드 표준): 데모 `#board`/`#cards` 클럽카드 = 썸네일(thumb-grad)+카테고리 핀(ink)+인증칩(brand-accent)+이름+한줄+모집상태(cta-grad/sand-100). Clubs·Home 공유. → **Clubs에서 표준 카드로 확정**하고 Home 캐러셀이 동일 비주얼 참조.
- **`ProjectCard`**: 데모 `#cards` 프로젝트카드 = 기업헤더(brand-tint)+카테고리핀+D-day칩+제목+활동비. B2BLounge·Home 공유.
- 신규 컴포넌트 남발 금지(메모리 feedback_builder_dev_style). 위 패턴을 리터럴 클래스로 적용.

---

## 1. Home (`src/pages/public/Home.tsx`)

**목적**: 운영진을 주인공으로 "동아리·학회 운영, 이제 한 곳에서"를 1초에 전달하고 `우리 동아리 시작하기`로 보낸다. 가치사다리 순서로 스크롤을 설계.

| # | 섹션 | 역할 | 핵심 카피 / CTA | 컴포넌트 |
|---|------|------|-----------------|----------|
| 1 | **Hero** | 운영=주인공 선언 + 주 CTA | Eyebrow "동아리·학회를 위한 운영 플랫폼" · H1 "동아리·학회 운영, / 이제 한 곳에서 시작하세요" · Sub(§6 그대로) · 주CTA "🚀 우리 동아리 시작하기 →" `/club-setup` · 보조 "동아리 찾는 학생이신가요? 동아리 둘러보기 →" `/clubs` · 우측 통계 2장(NumberTicker) | hero-glow, btn-grad CTA, stat-grad 카드, NumberTicker, FadeInText |
| 2 | **가치 3단(신규)** | 가치사다리 ①홈페이지 ②운영 ③자생을 카드 3장으로 명시 | "복잡한 웹빌더 없이, 우리 동아리 홈페이지" / "출석·회비·모집을 한 곳에서" / "기업과 진짜 프로젝트로 한 단계 더" | sand-200 카드 3장, lucide(Globe/ClipboardList/Briefcase, strokeWidth 2.5) |
| 3 | **마퀴(성과 띠)** | 결과 중심 사회증명 | 기존 4문장 유지(신입 3배·운영 70%·기업 프로젝트·회비 100% 공개) | brand-tint 띠 + ink 텍스트(검정띠→웜) |
| 4 | **동아리 캐러셀** | 2차(학생) 갈고리 | SectionHead "지금 활동 중인 동아리·학회" + "동아리 전체보기 →" `/clubs` · FilterChips · ClubCard | SectionHead, FilterChips, ClubCard |
| 5 | **스토리 캐러셀** | 운영 결과 서사 (STORY_ENABLED=false → 현행 숨김 유지) | "검증된 동아리들의 생생한 스토리" (게이트는 현상유지) | SectionHead, 스토리카드 |
| 6 | **기업 프로젝트 캐러셀** | 자생(마지막 스텝) | SectionHead "진행 중인 기업 프로젝트" + "프로젝트 전체보기 →" `/b2b` · ProjectCard | SectionHead, FilterChips, ProjectCard |
| 7 | **게이트웨이(2분기)** | 학생/기업 분기 | 좌 "동아리·학회 둘러보기" `/clubs` · 우 "기업 프로젝트 라운지" `/b2b` | cta-grad 블록 2장, lucide |
| 8 | **하단 배너** | 프로모션 | `getBanners('home')` | BannerSlider |

**현행 대비 변경점**
- 순서 변경: 가치사다리를 스크롤에 명시 → **§2 "가치 3단" 신규 삽입**(Hero 직후). 기존엔 가치가 마퀴/섹션에 흩어져 있었음.
- 캐러셀 순서: 동아리(②학생 갈고리) → 스토리 → **기업 프로젝트는 가장 마지막**(자생=확장 원칙). 기존도 유사하나 의미를 명확히.
- 전 구간 토큰 교체: `border-black`/하드섀도/`bg-orange-500`/`bg-black 마퀴` → sand/soft/btn-grad/brand-tint. 검정 게이트웨이 블록 → cta-grad.
- 카피는 §6 정본 유지(Hero 이미 일치). "오렌지 뱃지를 획득한" 류 → "인증을 마친 곳" 보조 표현으로 톤다운(이미 일부 적용됨).

---

## 2. Clubs — 동아리 찾기 (`src/pages/public/Clubs.tsx`)

**목적**: 2차 타겟(지원 학생)이 관심 분야 동아리·학회를 빠르게 찾아 상세로 진입. DESIGN_MASTER `#board`(검색+필터칩+정렬+그리드+페이지네이션)가 표준 레이아웃.

| # | 섹션 | 역할 | 핵심 카피 / 컨트롤 | 컴포넌트 |
|---|------|------|--------------------|----------|
| 1 | **페이지 헤드** | 제목 + 검색 + 카운트 | H2 "동아리·학회 둘러보기" · Sub "관심 분야의 동아리·학회를 찾아 둘러보세요." · "전체 N곳 · 지금 모집중 N곳" · 검색 placeholder "동아리 이름·키워드 검색" | FadeInText, field 검색바(돋보기) |
| 2 | **필터/정렬 바** | 카테고리 칩 + 모집중 토글 + 정렬 | 칩 "전체/마케팅·기획/IT·개발/창업/문화·예술" · "🔶 모집중만" 토글 · 정렬 select(최신·인기·마감임박) | FilterChips, Segmented/select |
| 3 | **결과 그리드** | 동아리 카드 목록 | ClubCard ×N (썸네일+핀+인증칩+이름+한줄+모집상태) | ClubCard 표준 |
| 3a | 로딩/빈/에러 | 상태 처리 | 스켈레톤 · "조건에 맞는 동아리가 없습니다" + "검색 초기화" · "동아리 목록을 불러오지 못했습니다." | EmptyState, 스켈레톤(sand-100) |
| 4 | **페이지네이션(선택)** | 다량 시 분할 | 활성=btn-grad, 나머지=border-sand-300 | 데모 `#board` 페이지네이션 |
| 5 | 관심등록 토스트 | 로그인 유도 | "관심 등록은 로그인 후 이용할 수 있어요." | 흰 토스트+상태 아이콘칩 |

**현행 대비 변경점**
- 카드를 **DESIGN_MASTER 표준 ClubCard로 확정**(이 페이지가 SSOT). Home 캐러셀이 이 비주얼을 참조.
- 모집상태 표기 통일: 모집중 = `cta-grad text-brand-dark "🔶 모집중 · D-N"`, 마감 = `bg-sand-100 text-sand-400 "모집 마감"`. (검정 뱃지+초록점 → 토큰)
- 인증 표식 = 우상단 `brand-accent` 체크칩(보조 신뢰 마크). "인증 동아리/미인증" 푸터 문구는 톤다운 유지.
- 필터칩 활성 `bg-black`→`bg-ink`, 토글 `bg-orange-500 text-black`→`bg-brand-tint text-brand-dark`. 대시 테두리 빈상태 → sand-200 점선.
- 정렬 컨트롤 추가(현행 없음, DESIGN_MASTER 기준). **신규 정렬 UI 추가 여부는 승인 항목** — 현행 유지(정렬 없음) 원하면 §2만 칩+토글로 둠.

---

## 3. ClubDetail (`src/pages/public/ClubDetail.tsx`)

**목적**: 단일 동아리의 소개·활동·모집을 보여주고 `지원서 작성`으로 전환. 운영진이 만든 1Page(ClubPageRenderer)가 있으면 그걸, 없으면 기본 레이아웃.

| # | 섹션 | 역할 | 핵심 카피 / CTA | 컴포넌트 |
|---|------|------|-----------------|----------|
| 1 | **커버 헤더** | 이름·유형·인증·한줄 | type 핀 · "인증 동아리"(보조) · H1 name · one_line_desc · "← 동아리 목록으로" | thumb-grad 커버, 핀, brand-accent 인증칩 |
| 2 | **사이드 탭** | 소개/모집/스토리 전환 | "소개" "모집" "스토리"(STORY_ENABLED) — **"채용"→"모집"으로 교체(§7 금지어)** | 세로 탭(흰+border-sand-200, 활성 brand) |
| 3 | **스티키 CTA 바** | 모집 정보 + 지원 | 모집중: "{기수} 모집중 (D-N)" + "{기수} 지원서 작성하기" `/clubs/{slug}/recruit` · 없음: "현재 모집 중인 공고가 없습니다" + "모집 페이지 보기" | 스티키 바, btn-grad CTA, Calendar |
| 4 | **본문(분기)** | 1Page 있으면 렌더, 없으면 기본 소개 | 커스텀: ClubPageRenderer · 기본: name + one_line_desc, (운영자) "1PAGE 웹 디자인으로 꾸미기" 유도 | ClubPageRenderer, 점선 유도 박스(brand-tint) |
| 5 | **활동 스토리** | 결과 서사 | "활동 스토리" + (운영자)"스토리 작성" · "전체보기(N개)" · 빈 "아직 등록된 활동 스토리가 없습니다." | 피처드 카드 + 스냅 그리드 |
| 6 | (운영자) 초안 배너 | 미발행 안내 | "초안 미리보기 — …" + "1PAGE 웹 디자인에서 발행하기 →" | warn 톤 배너(border 제거) |

**현행 대비 변경점**
- **§7 금지어 수정 필수**: 사이드 탭 "채용" → **"모집"**. 라우트(`/recruit`)·로직은 유지, 라벨만.
- "안전 검증 완료"(회전 뱃지) → **삭제**(브랜드 §5 폐기 카피). 인증은 "인증 동아리" 보조칩만.
- 스토리 탭 코드 중복(커스텀/기본 경로 동일 블록 2곳) — **공유 컴포넌트로 추출은 범위 밖**(외과적 수정 원칙). 토큰만 교체, 중복은 보고만.
- 전 구간 토큰 교체: `border-2 border-black`/하드섀도/`bg-orange-500`/`bg-yellow-400` → sand/soft/btn-grad/warn 토큰. 회전(`rotate-3`)·grayscale 호버 제거.
- 사이드 탭/스티키 바 hover 브루탈 플립(`hover:bg-black hover:translate-y-1 hover:shadow-none`) → soft lift.

---

## 4. B2BLounge — 기업 프로젝트 라운지 (`src/pages/public/B2BLounge.tsx`)

**목적**: 3차 타겟(기업)이 동아리·학회에 **프로젝트(협업)**를 제안. 자생 단계의 공개 입구. 공개 카피에서 "B2B/수주" 전면 금지(§7).

| # | 섹션 | 역할 | 핵심 카피 / CTA | 컴포넌트 |
|---|------|------|-----------------|----------|
| 1 | **Hero** | 기업 대상 제안 유도 | Eyebrow "기업 프로젝트 라운지"(COMPANY PROJECT LOUNGE 유지 가능) · H1 "동아리·학회에게 / 진짜 프로젝트를 제안하세요." · Sub(실무 과제+인증 보조) · CTA "우리 기업 프로젝트 등록하기 →" `/corp/register` · 통계 2장 | btn-grad CTA, stat 카드 (검정 hero→웜 또는 ink 면) |
| 2 | **성공 사례** | 사회증명 | "함께한 프로젝트" (←"매칭 성공 사례") · 카드 3장 — **"B2B SaaS"→"SaaS 서비스 사용성 개선"으로 수정** | 스냅 캐러셀, 케이스 카드 |
| 3 | **프로젝트 탐색** | 검색+필터+목록 | "프로젝트 탐색" · 검색 "프로젝트, 기업명 검색…" · FILTERS 칩 · ProjectRow ×N · 빈 "해당 …없습니다" + "전체 프로젝트 보기" | field 검색, FilterChips, ProjectRow |
| 3a | ProjectRow CTA | 제안/상세 | **"🔶 수주 제안하기"→"🔶 협업 제안하기"** · "상세 보기" · 마감 "모집이 완료되었습니다" | btn-grad CTA, 상태칩 |
| 4 | 상세 모달 | 프로젝트 상세 | title·카테고리·기업·마감·스킬·예산·설명 · **"이 프로젝트 수주 제안하기"→"협업 제안하기"** | Modal(갱신됨), MarkdownViewer |
| 5 | 계약 성격 배너 | 도급/책임 고지 | ContractNatureBanner(법무) | ContractNatureBanner |
| 6 | 하단 배너 | 프로모션 | getBanners('lounge') + 완료건수 동적 | BannerSlider |
| 7 | 토스트 | 권한 안내 | **"수주 제안은…"→"협업 제안은 동아리 운영진만 가능해요."** | 흰 토스트 |

**현행 대비 변경점 (⚠️ 금지어 수정 = 필수)**
- **"수주" 전면 제거**: ProjectRow CTA·모달 CTA·토스트 3곳 → "협업 제안하기"/"협업 제안"(§7: 수주→협업·도급). 라벨/카피만, 라우트·로직 유지.
- **"B2B SaaS"**(SUCCESS_CASES 데이터, 17–21줄) → "SaaS"로 수정. 공개 카피에서 "B2B" 제거.
- "매칭 성공 사례" → "함께한 프로젝트"(중개 어투 톤다운, §7 매칭 표현 완화).
- 검정 Hero/검정 성공사례 섹션 → 웜 톤. (ink 면은 강조 1곳만 허용, 나머지 흰/sand) — **검정 면 유지 여부 승인 항목**: 기본안=ink 면 1곳(Hero)만 강조, 성공사례는 흰 카드.
- 전 구간 토큰 교체: 하드섀도/`border-black`/`bg-orange-500`/`bg-green-400 점` → soft/sand/btn-grad/상태토큰. 상태 뱃지 = `statusColor`("모집중"=ok, "모집마감"=off).

---

## 5. 승인 요청 항목 (Phase C 착수 전 확인)

1. **Home §2 "가치 3단" 신규 섹션** 추가 — OK? (가치사다리 명시용, 신규 카드 3장)
2. **Clubs 정렬 컨트롤** 추가(최신/인기/마감임박) — 추가 vs 현행 유지(칩+토글만)?
3. **B2BLounge 검정 면 처리** — Hero만 ink 강조면 유지 + 성공사례는 흰 카드(기본안) vs 전부 흰/sand?
4. **금지어 수정**(필수, 비협상): ClubDetail "채용"→"모집", B2BLounge "수주"→"협업"·"B2B SaaS"→"SaaS" — 함께 처리 확인.
5. ClubDetail 스토리 탭 **코드 중복**은 이번 범위에서 토큰만 교체(추출 보류) — OK?

> 위 5개에 답을 주시면(또는 "기본안대로 진행") Phase C를 **Home→공유카드 확정→Clubs→ClubDetail→B2BLounge** 순(또는 페이지별 병렬)으로 진행합니다.

---

## 6. 승인 결과 (2026-06-24 확정)

1. Home "가치 3단" 신규 섹션 → **추가 안 함**. Home은 토큰 교체 + 섹션 순서 정리만(자생=마지막).
2. Clubs 정렬 컨트롤 → **추가**(최신/인기/마감임박 select + 정렬 로직).
3. B2BLounge 검정 면 → **Hero만 ink 강조 유지**, 성공사례·나머지는 흰/sand 카드.
4. 금지어 수정(필수): ClubDetail "채용"→"모집", B2BLounge "수주"→"협업"·"B2B SaaS"→"SaaS", "매칭 성공 사례"→"함께한 프로젝트".
5. ClubDetail 스토리 탭 코드 중복 → 토큰만 교체, 추출 보류.
6. 진행: **승인 · 페이지별 병렬**.
