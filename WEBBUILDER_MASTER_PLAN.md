# 1-Page 웹빌더 — 통합 마스터 플랜

> **이 문서가 최신 단일 출처(SSOT)다.** `WEBBUILDER_ANIMATION_PLAN.md`(스크롤 연출 카탈로그)와 `WEBBUILDER_TEMPLATES_PLAN.md`(템플릿 3계층)는 **세부 카탈로그·부록**으로 유지하되, 우선순위·범위·방향은 본 문서가 덮어쓴다.
> 작성: 2026-06-04. 근거: 나인하이어/아임웹 실제 빌더 캡처 분석 + 기존 두 플랜 + 현재 코드(`blockKit.tsx`·`BlockPropertiesPanel.tsx`·`Workspace.tsx`·`sectionTree.ts`).

---

## 0. 한 줄 결론 (제품 포지셔닝)

> **목표: 아임웹보다 가볍게, 나인하이어보다 예쁘게.**
> 이 둘은 모순이 아니다. 해답은 **"아름다움을 사용자가 설정하게 하지 않는 것"** — GSAP 스크롤 연출·텍스트 리빌·정교한 모션은 **템플릿과 위젯 프리셋에 미리 박아두고**, 비전공자 사용자는 토글·슬라이더 몇 개만 만진다. 도구는 나인하이어처럼 단순한데 결과물은 아임웹 이상으로 나온다.

3대 기둥:
1. **단순한 도구** — 통일된 UI, 정돈된 우측 패널, 모바일 자동 배치(편집 없음).
2. **박아둔 아름다움** — 위젯 디자인 프리셋 + GSAP 스크롤 연출(Lenis 관성 + ScrollTrigger).
3. **검증 가능한 목표** — 기존 위젯만으로 **새 데모 동아리 소개 페이지**를 "이만큼 예쁘게" 완성한다.

---

## 1. 세 빌더 벤치마크 종합 (무엇을 가져오고 무엇을 버리나)

| 항목 | 나인하이어 | 아임웹 | 우리(현재) | 우리 채택 |
|---|---|---|---|---|
| 데이터 구조 | 섹션→레이아웃→열→위젯 | 동일 | 동일([sectionTree.ts](src/lib/sectionTree.ts)) | ✅ 유지(변경 0) |
| 우측 패널 | 컨텍스트형(떴다 사라짐) | 고정+탭(기본/마우스오버) | 고정([BlockPropertiesPanel.tsx](src/components/admin/BlockPropertiesPanel.tsx)) | **고정 유지 + 토글 + 정돈**(Track A) |
| 통일감 엔진 | 미니멀(위젯 적음) | 공통 디자인 설정(전역 토큰) | 테마 색만([WorkspaceProperties.tsx](src/components/admin/WorkspaceProperties.tsx)) | 🔶 부분(프리셋이 테마 상속) |
| 위젯 디자인 | 깔끔 기본형 | 프리셋 다수 | 12종+버튼12·FAQ6 프리셋 | **위젯 디자인 프리셋 확장**(Track C) |
| 애니메이션 | 카드 3종 | 카드 8종 | CSS 9종+글자리빌+KenBurns | **GSAP 스크롤 연출 승격**(Track B) |
| 컬럼 리사이즈 | 열 사이 `‹›` 드래그 | 12그리드 | 프리셋만 | 🔸 후순위 |
| 이미지 마스킹 | 모양 크롭(원형·3:2…) | 채우기/모서리 | 없음 | 🔸 후순위(프리셋에 일부 흡수) |
| 모바일 | 자동 스택+미리보기 | 분리 편집(한줄표시 N개) | container-query 자동([blockKit.tsx:144](src/components/blockKit.tsx#L144)) | **자동만, 편집 UI 없음**(Track D) |
| 요소 ID 노출 | 있음 | 있음 | 없음 | ⛔ **영구 미도입** |
| 템플릿 갤러리 | 섹션 8카테고리 | 콘텐츠 프리셋 갤러리 | 시드 3종 | 🔸 **후순위**(위젯 먼저) |

범례: ✅ 즉시 · 🔶 부분 · 🔸 후순위 · ⛔ 안 함

---

## 2. 이번 라운드 확정 사항 (대화 2026-06-04)

### 2-1. 버린다 / 미룬다
- ⛔ **요소 ID(섹션ID·위젯ID) 노출 — 영구 미도입.** (앵커·연동 수요 없음)
- ⛔ **모바일 분리 편집 — 안 만든다.** 모바일은 container-query **자동 배치**가 최선. 에디터엔 **읽기전용 미리보기 토글**만.
- 🔸 **섹션/페이지 템플릿 갤러리 UI — 후순위.** "일단 위젯들로만 잘 만든다"가 먼저.
- 🔸 컬럼 드래그 리사이즈, 이미지 모양 마스킹 — 후순위(여력 시).

### 2-2. 지금 한다 (확정 4트랙)
- **Track A — 우측 패널 정돈** (체감 1순위): 고정 패널 유지 + **접기/펼치기 토글** + **부드러운 전환 애니메이션** + **핵심/세부 시각 위계 재설계**.
- **Track B — 위젯 애니메이션**: GSAP+ScrollTrigger lazy(애니플랜 P2 승격) + Lenis(완료) 연계.
- **Track C — 위젯 디자인 프리셋**: 우측 패널 in-panel 프리셋(템플릿플랜 §12).
- **Track D — 검증**: 기존 위젯만으로 **새 데모 동아리 소개 페이지** 저작 = 성공 기준.

### 2-3. 라이브러리 역할 분담 (확정)
- `motion@12`(설치됨·미사용) → **에디터 UI 전환**(패널/모달 슬라이드·페이드)에 사용. 발행 페이지 번들과 무관 → 방문자 비용 0.
- `lenis`(설치+P1 완료) → 발행 페이지 관성 스크롤(토글).
- `gsap`(신규) → 발행 페이지 스크롤 연출, **Tier-2 lazy import**(쓰는 페이지에서만). 2025년 전 플러그인 무료화로 도입 장벽 없음.

---

## 3. Track A — 우측 패널 정돈 (사용자 최우선 체감)

> **진행 상태(2026-06-04): 3-1·3-2 완료, 3-3 1차(중앙 프리미티브) 완료.**
> - 3-1 접기/펼치기: `Workspace.tsx` 우측 패널을 얇은 핸들 바(`‹`/`›`) + width 슬라이드로 감쌈. 상태 `localStorage('wb-panel-open')`. 접으면 `main`(flex-1)이 풀폭.
> - 3-2 부드러운 전환: `motion@12`(`motion/react`)로 패널 등장/이탈(width+opacity), 위젯 선택/탭 전환 시 패널 내용 페이드(`BlockPropertiesPanel`·`WorkspaceProperties`). `useReducedMotion` 가드(즉시 전환).
> - 3-3 시각 위계: 공용 `Section`(그룹 헤더=진한 굵은 글씨+좌측 브랜드 액센트 바)와 `Label`(연회색 캡션)을 **차등화**해 한 곳에서 전 위젯 패널에 일괄 적용. (한국어 타이틀은 uppercase 무의미 → 색만 같아 평평했던 문제 해소.)
> - **후속(3-3 심화):** 위젯별 '핵심 상시노출 / 세부 아코디언' 재배치는 점진 적용(현 Section 아코디언 메커니즘 위에 위젯 단위로). Track C 'in-panel 프리셋 행'은 별도 트랙.

> 증상(사용자 직접): ① 패널이 고정이라 캔버스가 좁다 ② 패널 진입/이탈·탭 전환에 **부드러운 연결이 없다** ③ **핵심 항목과 세부 항목이 시각적으로 구분 안 된다** ④ 정보 밀도가 높아 복잡해 보인다.

### 3-1. 접기/펼치기 토글
- 패널 좌측 가장자리에 핸들 버튼(`‹` / `›`). 접으면 캔버스 풀폭, 펼치면 패널 복귀.
- 상태 기억(localStorage). 좌측 위젯/템플릿 레일도 동일 패턴 적용 검토.

### 3-2. 부드러운 전환 (motion 사용)
- 패널 mount/unmount: slide-in(x: 24→0) + fade, ~180ms ease-out.
- 탭 전환(content/design/motion): 내용 크로스페이드 + 높이 자동.
- 위젯 선택 변경 시 패널 내용 교체도 같은 트랜지션 → "딸깍 바뀜" 제거.
- `prefers-reduced-motion` 준수(즉시 전환).

### 3-3. 시각 위계 재설계 (가장 중요)
나인하이어/아임웹의 "깔끔함" 핵심은 **그룹핑 + 점진적 공개**다.
- **3단 위계 고정**: `섹션 헤더`(굵게+구분선) → `항목 라벨`(중간 굵기·회색) → `컨트롤`. 현재처럼 평평한 나열 금지.
- **핵심/세부 분리**: 자주 쓰는 항목(정렬·크기·색)만 상단 상시 노출, 세부(자간·외곽선·threshold 등)는 **접이식(아코디언) 또는 "고급" 섹션**으로 숨김.
- **프리셋 우선, 커스텀 옵트인**: 여백 등은 `좁게/보통/넓게/커스텀` 세그 먼저 → 커스텀 선택 시에만 슬라이더 노출(아임웹 `기본 vs 직접 지정` 패턴).
- **패널 최상단에 "디자인 프리셋" 행**(Track C 진입점).
- 라벨·간격·구분선 토큰을 패널 전역에서 통일(현재 위젯마다 산발적).

### 3-4. 검증
- 캔버스 폭: 패널 접힘 시 100% 사용.
- 같은 위젯 3종을 열어 패널 구조(헤더/라벨/컨트롤 위계)가 **동일 규칙**으로 보이는지.
- 기존 속성 편집 기능 회귀 없음(모든 필드 그대로 동작).

---

## 4. Track B — 위젯 애니메이션 (GSAP 승격)

> **진행 상태(2026-06-04): §4-1 인프라 + §4-2 섹션 패럴랙스(첫 GSAP 연출) 완료.**
> - GSAP+ScrollTrigger **lazy 로더** `src/lib/scrollFx.ts`: `loadScrollFx()`가 `import('gsap')`/`import('gsap/ScrollTrigger')`를 동적 로드(캐시). 패럴랙스가 켜진 공개 페이지에서만 로드 → 미사용 시 번들 0kb. `prefers-reduced-motion` 가드.
> - 공용 `useInView` 훅 `src/lib/useInView.ts`(IntersectionObserver, 위젯 재사용용).
> - **섹션 배경 패럴랙스**: 패널 '배경(이미지)'에 `배경 패럴랙스` 슬라이더(`bgParallax`, 0=끔). 공개 `SectionBlock`이 별도 배경 레이어를 scrub 이동(Ken Burns보다 우선). 에디터는 정적 미리보기(스크롤 없음). 레이어 160% + 이동 ×0.45 스케일로 가장자리 공백 방지.
> - **후속:** Sticky pin 단계전개·텍스트 SplitText 고도화는 동일 `scrollFx` 훅 패턴으로 증설. 패럴랙스를 section 프리셋에 내장하는 것도 후속(현재는 toggle).

> 출처: `WEBBUILDER_ANIMATION_PLAN.md` (P0·P1 완료). 본 트랙은 **P2(GSAP lazy)를 최우선으로 승격**. 카탈로그·레퍼런스 실측은 그 문서 §2·부록 참조.

### 4-1. 공통 인프라
- GSAP+ScrollTrigger **lazy 로더**: 섹션에 스크롤 연출이 하나라도 켜진 페이지에서만 동적 import(안 쓰면 0kb).
- `useInView` 공용 훅(StatsBlock IO 로직 추출) — 모든 위젯 재사용.
- `prefers-reduced-motion` 전역 가드.

### 4-2. "박아두는" 연출 (사용자는 토글만)
| 연출 | 적용 위젯 | 사용자 노출 |
|---|---|---|
| 스크롤 진입 리빌(고도화) | 전 위젯 | 프리셋에 기본 포함 |
| 텍스트 라인/단어/글자 리빌(SplitText급) | text | 프리셋 "히어로 헤드라인"에 내장 |
| 섹션 parallax / Ken Burns | section/image | 토글 1개 |
| Sticky pin 단계 전개 | section | 프리셋/토글 |
| 카운트업·마퀴(보유 고도화) | stats·divider | 슬라이더 |

> 원칙: 새 위젯 0개. 전부 기존 위젯 + 패널 토글/슬라이더. ([[feedback_builder_dev_style]])

---

## 5. Track C — 위젯 디자인 프리셋 (in-panel)

> **진행 상태(2026-06-04): ★프리셋 골격 완료.**
> - `src/lib/widgetPresets.ts` 신설 — `WidgetPreset{ id,label,desc,patch(accent) }` + `WIDGET_PRESETS` 레지스트리. **text·stats·timeline·divider·image·section** 각 2~4종(★중심, 총 19종). 디자인 필드만 `__merge` 패치 → 텍스트·항목 데이터 보존, accent=테마색 주입.
> - 패널 **최상단 '디자인 프리셋' 행**(`WidgetPresetRow`+`WidgetPresetModal`, FAQ 모달 패턴 일반화). 프리셋 보유 위젯에서만 노출. Track B 등장/리빌이 text·timeline·stats 프리셋에 내장됨.
> - button·faq 는 기존 전용 모달(`BTN_TEMPLATES`·`FAQ_TEMPLATES`) 유지(중복 UI 방지). '단일 출처' 흡수는 후속(저위험 리팩터로 보류).

> 출처: `WEBBUILDER_TEMPLATES_PLAN.md` §12. 메커니즘은 이미 절반 구현(버튼 12종 `BTN_TEMPLATES`, FAQ 6종 `FAQ_PRESETS`). **이 패턴을 다른 위젯에 복제**.

- `src/lib/widgetPresets.ts` 단일 레지스트리: 위젯 타입별 `WidgetPreset[]`. 기존 버튼·FAQ 프리셋도 여기로 흡수(단일 출처).
- 프리셋 = **디자인 필드만** 번들 패치(`__merge`). 텍스트·항목 데이터 보존. accent는 현재 테마 hex 주입 → 테마 바꾸면 프리셋 톤 따라감(부분 통일감 엔진).
- 우선 제작(위젯당 2~3종): **text·stats·timeline·divider·image·section** ★프리셋. Track B의 애니메이션이 프리셋에 내장됨(아름다움 박아두기).
- UI: Track A의 패널 최상단 "디자인 프리셋" 행(버튼 템플릿 모달 UX 재사용) — 칩 미리보기 + 선택.

---

## 6. Track D — 검증: 새 데모 동아리 소개 페이지

> **진행 상태(2026-06-04): 2종 신규 저작 완료(미적용).**
> - `supabase/migrations/20260604080000_seed_codewave_frame_redesign_v5.sql` — **기존 디자인 미사용, 완전 신규 기획**.
>   - 코드웨이브: **라이트 블루프린트 에디토리얼**(화이트/아이보리 + 인디고 #4f46e5, IBM Plex Sans, 비대칭 1:2 그리드, accentTop 3카드, 다크 스탯 밴드, **bgParallax 쇼케이스(Track B)**, 빅넘버 가로 타임라인, line FAQ, 인디고 CTA). 11블록.
>   - 프레임: **웜 필름 갤러리**(아이보리 #f7f3ec + 잉크/앰버 #b45309, 함렛 명조, **bgParallax 필름 히어로(Track B)**, 이미지 3연작, 잉크 스탯, KenBurns 인용 밴드, 세로 ring 타임라인, boxed FAQ, 앰버 CTA). 10블록.
>   - 둘 다 기존 시드(다크테크 v4 / 모노벤토)와 팔레트·폰트·레이아웃 전부 상이. 신규 위젯 0, Track A~C 프리셋 필드값 + Track B 패럴랙스 사용.
> - **미적용:** DB가 원격(프로덕션)이고 마이그레이션이 기존 발행 페이지를 DELETE·재시드(파괴적) → 사용자 확인 후 적용. JSON·구조 검증은 통과.

> 성공 기준(CLAUDE.md §4): "잘 만든다"를 **하나의 완결된 산출물**로 고정.

- **기존 12위젯 + Track A~C 결과물만** 사용해, 가상의 동아리로 **풀 동아리 소개 페이지**를 저작.
- 골격: `히어로(KenBurns+텍스트리빌) → 소개 → 활동/프로그램(카드) → 통계 → 프로세스(timeline) → FAQ → 모집 CTA(countdown+button)`.
- 이 페이지가 **나인하이어 SEVI 캡처보다 확실히 예쁘면** Track A~C는 성공으로 본다.
- 부수효과: 이 데모는 나중 §10 섹션 템플릿의 원본이 된다(후순위 트랙으로 자연 연결).

---

## 7. 로드맵 (순서·검증)

```
1. Track A 패널 정돈        → 검증: 캔버스 풀폭, 위계 일관, 기능 회귀 0
2. Track C 위젯 프리셋 골격  → 검증: button·faq 흡수 + text/section ★프리셋 적용
3. Track B GSAP lazy 로더    → 검증: 미사용 페이지 0kb, reduced-motion off
4. Track B 핵심 연출(리빌·parallax·pin)을 프리셋에 내장
5. Track D 데모 동아리 페이지 저작 → 검증: SEVI 대비 시각 우위
── 이후(후순위) ──
6. 섹션/페이지 템플릿 갤러리(템플릿플랜 §10) · 컬럼 드래그 리사이즈 · 이미지 마스킹
```

검증 공통:
- 모든 신규는 **optional**(미설정 위젯은 현재와 동일 렌더 = 기존 발행 페이지 회귀 0).
- 발행 페이지 번들: GSAP/Lenis lazy 검증.
- `tsc --noEmit` 클린 + dev 서버 시각 확인. ⚠️ 프로덕션 `vite build`는 이 머신에서 네이티브 크래시 이력 있음 → dev 서버로 QA.

---

## 8. 부록: 기존 문서와의 관계
- `WEBBUILDER_ANIMATION_PLAN.md` → 본 문서 Track B의 **상세 카탈로그·25개 레퍼런스 실측**. P0·P1 완료분 기록 유지.
- `WEBBUILDER_TEMPLATES_PLAN.md` → 본 문서 Track C(§12 위젯 프리셋)의 **상세 설계** + 후순위 템플릿 갤러리(§10) 보관.
- 본 문서가 **우선순위·범위·방향의 최종 결정권**을 갖는다.
