# 운영진 워크스페이스 통합 기획 (Ops Workspace Plan)

> **목적:** 새로 받은 3개 기획 소스(부원 명단 / 출석·세션 / 모집·지원 사스 기획안)를 **기존 구현 상태와 대조**하여, 무엇이 이미 되어 있고 / 무엇이 신규 작업이고 / 무엇이 결정 대기인지 한곳에 정리한 SSOT.
> **작성 원칙:** 기존 테이블·정책·컴포넌트를 최대한 활용하여 *추가*한다. (recruit.md 의 핵심 전제 계승)
> **검증 기준일:** 2026-06-04 (코드/스키마 실측 기반)

## 소스 문서
1. **부원 명단 대시보드 가안** (HTML mock + PDF p13~19) — 명단 뷰/필터/커스텀필드/일괄변경/대시보드
2. **출석 및 세션 관리 가안** (HTML mock + PDF p20~29) — 세션 IA 분리/예외인원/출석상태 수정/**출석 인정 신청·승인(신규)**
3. **아워클럽 사스 기획안 PDF** (p1~12) — 모집/지원 커스터마이징·파이프라인·대시보드·분석

## 상태 범례
- ✅ **구현됨** — 이미 동작. 추가 작업 불필요(또는 미세 폴리시)
- 🟡 **부분구현** — 뼈대 있음, 확장 필요
- 🔴 **신규** — 없음. 신규 개발 필요
- ❓ **결정필요** — 방향이 갈리거나 사용자 확인이 필요

---

## 1. 현재 구현 상태 한눈에

| 도메인 | 핵심 페이지 | 한줄 상태 |
|--------|------------|-----------|
| 모집/지원 | `RecruitmentsList/Detail`, `ApplicantsTab`, `RecruitPageBuilder`, `RecruitDashboard`, `RecruitAnalytics` | **~90% 완성.** 폼빌더(10타입)·칸반·평가/면접/팀채팅·이메일자동화·채용홈빌더·대시보드·분석 모두 존재. → **폴리시 중심** |
| 부원 명단 | `MembersAdmin`, `members/*` | **~60%.** 명단/기수picker/커스텀필드(텍스트전용)/일괄저장/일괄상태변경/엑셀/기본차트 존재. → **타입시스템·출석률기준·분석 고도화 필요** |
| 출석/세션 | `AttendanceCreate/List/Detail`, `user/sections/Attendance*` | **~55%.** IA분리·코드·마감·예외인원·상태수정(출석/지각/결석/공결)·학생체크인 존재. → **출석인정 신청·승인 워크플로(신규)가 핵심** |

> 결론: 모집/지원은 거의 끝났고, **신규 개발의 무게중심은 (1) 출석 인정 워크플로 와 (2) 명단 고도화**에 있다.

---

## 2. 출석 / 세션 관리

### 2.1 이미 된 것 ✅
- IA 분리: **세션 출석기록 생성**(`/admin/sessions/new`) / **전체 세션 관리**(`/admin/sessions`) — 기획안 1번 IA 변경 이미 반영됨
- 새 세션: 이름·기수·**출석 마감시간(10/15/30분/직접입력)**·**예외인원(대상 제외)** 선택·4자리 코드 발급·실시간 출석 카운터 — `AttendanceCreate.tsx`
- 전체 세션 관리: 기간·기수 필터, 출석현황 %, 엑셀 다운로드, 상세보기 — `AttendanceList.tsx` (`sessions_with_counts` 뷰)
- 세션 상세: 개별 출석상태 **드롭다운 수정**(`출석/지각/결석/공결`), 변경행 하이라이트+일괄저장+확인모달 — `AttendanceDetail.tsx`
- 학생: 코드 입력 체크인 / 출석 이력 — `user/sections/AttendanceSection.tsx`, `AttendanceHistorySection.tsx`
- DB: `sessions`(`target_generations`, `expires_at`, `attendance_code`), `session_targets`(모수), `attendances`(status CHECK 출석/지각/결석/공결)

### 2.2 신규 / 확장
| 항목 | 상태 | 내용 |
|------|------|------|
| **출석 인정 신청·승인 워크플로** | 🔴 신규 | **doc #2의 핵심.** 학생 마이페이지 → 출석인정 신청(일자·사유카테고리·상세·파일첨부) → 운영진 검토(달력뷰 + 신청목록, 승인/반려/취소). 신규 테이블 필요(§5.1). |
| 예외인원 **기수+활동상태 필터** | 🟡 부분 | 현재 `활동중` 전원만 로드. 기획안: 전체기수 옆 활동상태 필터 추가(수료 선택은 드묾). `AttendanceCreate` 예외 선택 UI에 필터 추가 |
| 승인 → **자동 예외 처리 연동** | 🔴 신규 | 출석인정 승인 시 해당 세션에서 자동으로 ❓처리(§결정 D5). "예외인원 자동 분류 안 되어도 아카이빙되니 유용" (doc) |
| 학생 이력에 **공결 표시** | 🟡 부분 | `AttendanceHistorySection` statusColor에 `공결` 누락 → 색상 추가(소규모) |
| 세션 상세 출석상태 옵션에 **출석인정(공결)** 명시 | ✅ | 이미 `공결` 존재. 라벨만 "출석인정(공결)"로 통일 검토 |

### 2.3 결정필요 ❓
- **D4. 출석 인정 사유 카테고리 확정**: (mock 기준) 개인 일정 / 병가 / 교내 일정 / 자격증 시험 / 가족 행사 / 기타 — 이대로 갈지
- **D5. 승인 시 출석률 처리 규칙**: 출석인정을 ⓐ *분자(출석)로 카운트* / ⓑ *분모에서 제외(모수 제외)* / ⓒ *공결 상태로만 기록(분자·분모 영향 없음, 별도 표기)* — **계산 정의가 명확해야 명단 출석률과 일치**

---

## 3. 부원 명단 관리

### 3.1 이미 된 것 ✅
- 명단 컬럼: 이름(+역할뱃지)·학교·기수·출석률(바)·상태 + 커스텀필드 — `MembersAdmin.tsx`
- 현재 활동기수 picker(`clubs.current_generation`) + "기수 마감"(전원 수료 처리) + 기수목록관리 모달
- 탭: 현재 활동기수 / 전체·이전기수 / 합류신청
- 커스텀필드: 추가·숨김·삭제(`club_custom_fields`/`club_member_custom_values`) — **단, 텍스트 타입 전용**
- 다중 컬럼 필터(기수/상태/커스텀) + 검색
- **일괄저장**: auto-save 폐기됨. 변경행 주황(`수정됨`) + 하단 저장바 + 확인모달 — 기획안 4번 이미 반영
- **일괄변경**: 체크박스 열 + 선택 시 액션바(상태 변경) — 기획안 5번 부분 반영
- 엑셀 다운로드(필터 반영)
- 기수별 평균 출석률 막대그래프(1종)
- 부원 추가: 이메일 초대 / 계정없이 추가 모달 — `InviteModal`
- 합격→자동 명단등록 트리거 `promote_applicant_to_member`

### 3.2 신규 / 확장
| 항목 | 상태 | 내용 |
|------|------|------|
| **커스텀필드 타입 시스템** | 🔴 신규 | 현재 텍스트 전용. mock은 단답/장문/숫자/단일선택/다중선택/드롭다운/날짜/전화/이메일/URL/파일 등. `club_custom_fields`에 `field_type`(+옵션 JSON) 추가 필요(§5.2). **v1 타입 범위는 ❓D6** |
| 커스텀필드 **순서변경 UI + 하위옵션(계층)** | 🟡 부분 | `display_order` 컬럼 존재하나 UI 없음. mock은 옵션 하위에 sub-옵션 트리(예: 휴학중 > 일반휴학/군휴학) |
| **상태기반 출석률 + 기수 아카이빙** | 🔴 신규 | 현재 출석률 = 전체 attendances(상태 무관). 기획: *활동중=현 학기 실시간 / 수료·이전기수=기수별 최종 출석률 아카이빙(누적)*. **데이터모델 결정 필요 ❓D2** |
| **필터별 인원수(총 N명)** | 🔴 신규 | 필터 조합 결과 하단에 "총 N명". (다중필터 핵심 요청) |
| 기수 뷰 **탭→드롭다운 통합** | ❓ | 기획: 현/이전 탭 대신 *기수 드롭다운 단일화 + (현재) 마커 + 내림차순*. 현재는 3탭. **D3** |
| 일괄변경 **역할 변경 추가** | 🟡 부분 | 현재 상태 변경만. mock은 역할 변경 + 상태 변경 |
| **명단 분석 대시보드 고도화** | 🔴 신규 | 현재 막대그래프 1종. 기획 지표: 수료율·평균출석률·중도이탈률 / 상태분포·휴식→복귀전환율·기수간리텐션 / 회차별출석분산·출석률구간분포(히스토그램) / 개인 streak. SQL 로직은 PDF p25~29(앱스크립트 참고). |
| **합격자 일괄 끌어오기** | 🔴 신규 | 합격자 화면에서 다중선택→명단 추가. (PDF 참여자관리: 여러 기수 합격처리·휴식복귀 대응) |
| 권한분리(Privacy View) | ⏸️ 보류 | 일반부원/초대 화면 PII 마스킹 — doc도 "권한분리 어려워 홀드" |

### 3.3 결정필요 ❓
- **D1. 부원 초대 기능 존치 여부** — *기획안 내부 의견 충돌.* 예빈: "리크루팅으로 충분, 초대 삭제" / 채연: "휴식복귀·여러기수 합격 대응 위해 추가 버튼 유지". (합격→자동등록 트리거는 이미 있음)
- **D2. 출석률 계산·아카이빙 모델** — ⓐ *기수마감 시 최종 출석률 스냅샷 테이블에 저장* / ⓑ *세션의 `target_generations`·일자로 매번 계산(아카이빙 테이블 없이 기수 스코프 쿼리)*. 엑셀식 `if(상태=활동중, 현학기식, 수료식)` 구현 방식 확정 필요.
- **D3. 기수 뷰 구조** — 현행 3탭 유지 vs 기수 드롭다운 단일화(+(현재)마커, 내림차순)
- **D6. 커스텀필드 v1 타입 범위** — 전체(11종) vs 핵심(단답/장문/숫자/단일선택/날짜) 우선

---

## 4. 모집 / 지원 (대부분 완성 → 폴리시)

### 4.1 이미 된 것 ✅ (기획안 PDF 대부분 충족)
- IA: 전체 채용 / 모집 대시보드 / 분석 리포트 (사이드바) + 채용 메인페이지 빌더 + 공고/지원서 — `AdminSidebar`
- 공고: 필수정보 모달 → 상세(마크다운 본문) → 폼빌더 → 파이프라인 (4탭 `RecruitmentDetail`)
- 폼빌더 10타입(단답/장문/숫자/이메일/전화/단일/다중/파일/지원경로/동의) + 필수토글 + 드래그정렬 + 임시저장/배포(버전) — 기획 "기본항목+타입추가" 충족
- 기본항목: 이름/연락처/이메일(정규식 검증)/지원경로(SNS·홈페이지·에타·링커리어·기타)/개인정보동의 — 충족
- 파이프라인: 단계 커스텀(추가/삭제/이름/순서) 기본값 서류-인터뷰-합격, 칸반 D&D, 단계이동 시 이메일 자동화 모달(`club_email_templates`,`application_stage_log`)
- 지원자 상세 모달: 접수일자(상단)·개인정보·상태 드롭다운·**태그**·탭(지원서/평가/팀채팅/면접질문) — 기획 충족
- 채용 메인페이지 빌더 5섹션: hero(슬로건+썸네일+위치)/공고리스트/스토리/후기·평점/FAQ + 브랜드컬러 — `RecruitPageBuilder`
- 모집 대시보드: 총지원/진행중/합격/불합격 + 단계별분포(불합격률 포함) + 공고별분포 — `RecruitDashboard`
- 분석 리포트: 총지원/최종합격률/주간유입그래프/지원경로별/문항응답분포 — `RecruitAnalytics`
- 지원자 임시저장: 미적용(=기획안 "지원자는 외부인, 안 함" 일치)
- CSV/엑셀 내보내기

### 4.2 신규 / 확장 (폴리시)
| 항목 | 상태 | 내용 |
|------|------|------|
| 후기·평점 **잡플래닛형 강화** | 🟡 부분 | 현재 `club_reviews` 표시/공개토글. 기획: 재직(활동)연도 필터 + 항목별(복지/워라밸 등 동아리版) 평균별점 + 추천율 도넛. 동아리 맥락 항목 정의 필요 |
| 공개페이지 **선발 프로세스 타임라인** | 🟡 부분 | 공고에 서류-인터뷰-합격 타임라인 시각화(메인/공고 노출) |
| FAQ **아코디언→토글** | 🟡 부분 | mock 코멘트 반영(미세) |
| 기본정보+신청버튼 **우측 배치** | 🟡 | 공개 공고 레이아웃 좌(본문)/우(기본정보+지원하기) |
| TRM(채용 캘린더 등) | ⏸️ 보류 | 기획안도 "고도화는 나중에" |

### 4.3 결정필요 ❓
- **D7. 후기 평점 항목 세트** — 잡플래닛(복지/워라밸/사내문화/승진/경영진)을 동아리 맥락으로 재정의할지(예: 활동만족/운영진/분위기/성장/부담), 아니면 단일 별점 유지

---

## 5. 데이터 모델 변경 제안 (초안 — 결정 후 확정)

> 모두 기존 RLS 패턴(`club_members.role='운영진' AND club_id` 또는 `global_admins`) 계승. FK 인덱스 동반.

### 5.1 출석 인정 (신규 테이블) — §2.2 연동
```
attendance_excuse_requests
  id uuid PK
  club_id uuid → clubs
  member_id uuid → club_members        -- 신청 부원
  session_id uuid → sessions (nullable) -- 특정 세션 매칭(없으면 일자 기준)
  excuse_date date                      -- 인정 요청 일자
  reason_category text                  -- §D4 카테고리
  detail text
  file_url text (nullable)              -- Storage(certification-docs 재사용 검토)
  status text  -- '대기'|'승인'|'반려'|'취소'
  reviewer_note text
  reviewed_by uuid / reviewed_at timestamptz
  created_at timestamptz
  idx: (club_id,status,created_at), (member_id), (session_id)
```
- 승인 트리거(선택): 승인 시 매칭 세션 `attendances.status='공결'` upsert + (D5에 따라) `session_targets` 제외 처리.

### 5.2 커스텀필드 타입 — §3.2 연동
```
ALTER club_custom_fields ADD field_type text DEFAULT 'text'  -- text|textarea|number|select|multiselect|date|phone|email|url|file
ALTER club_custom_fields ADD options jsonb DEFAULT '[]'      -- 선택형 옵션(+ 하위옵션 트리)
ALTER club_custom_fields ADD required boolean DEFAULT false
```
- `club_member_custom_values.value text` 유지(다중선택은 JSON 직렬화) 또는 `value jsonb`로 확장 검토.

### 5.3 출석률 아카이빙(D2-ⓐ 채택 시) — §3.2 연동
```
member_generation_stats   -- 기수 마감 시 스냅샷
  member_id / cohort(generation) / final_attendance_rate / completed_at / status_at_close
```
- D2-ⓑ 채택 시: 신규 테이블 없이 `attendances`×`sessions.target_generations`/일자 스코프 집계.

---

## 6. 결정 목록 (통합)

> ✅확정 = 2026-06-04 사용자 확정. ❓미정 = 추가 확인 필요.

| ID | 도메인 | 결정 | 결론 |
|----|--------|------|------|
| **D1** | 명단 | 부원 초대 기능 존치/삭제 | ✅ **존치 + 합격자 일괄끌어오기 추가** |
| **D2** | 명단 | 출석률 계산·아카이빙 모델 | ✅ **ⓑ 스코프 쿼리** (별도 아카이빙 테이블 없음). 활동중=현 기수 실시간, 수료=과거 기수 스코프 집계 |
| **D3** | 명단 | 기수 뷰 구조 | ✅ **기수 드롭다운 단일화** (+(현재) 마커, 내림차순). 3탭 폐기 |
| **D4** | 출석 | 출석인정 사유 카테고리 | ❓ 미정 → 기본값: mock 6종(개인일정/병가/교내일정/자격증시험/가족행사/기타) |
| **D5** | 출석 | 출석인정 출석률 처리 | ✅ **ⓐ 분모 유지·분자 제외**. 공결은 `session_targets`(분모)에 남고 `attended_count`(분자=status='출석')엔 미포함 → 출석률 하락. 현 `sessions_with_counts` 뷰 변경 불필요 |
| **D6** | 명단 | 커스텀필드 v1 타입 범위 | ❓ 미정 → 기본값: 핵심5(단답/장문/숫자/단일선택/날짜) 우선, 점진 확장 |
| **D7** | 모집 | 후기 평점 항목 세트 | ❓ 미정 → 기본값: v1 단일별점, 다항목(잡플래닛형)은 후순위 |

> D4/D6/D7은 기본값으로 진행 가능(저위험). **D5는 M1 구현 직전 확정 권장**(출석률 정의 직결).

---

## 7. 우선순위 (마일스톤) — 확정 순서

> 사용자 확정: **M1(출석 인정)부터 시작.** 모집/지원 폴리시는 후순위.

1. **✅ M1 — 출석 인정 워크플로(구현 완료, DB 적용 대기)**:
   - 마이그레이션 `20260604070000_attendance_excuse_requests.sql` — 테이블+RLS(본인/운영진/마스터)+`approve_excuse_request` RPC(승인+공결 upsert)+`notify_excuse_status` 알림 트리거
   - 학생: `AttendanceSection.tsx`에 출석 인정 신청 패널(동아리/일자/사유6종/상세/파일첨부) + `AttendanceHistorySection.tsx`에 신청 내역·공결 표시
   - 운영진: `AttendanceExcuses.tsx`(달력뷰 + 신청목록, 상태필터, 승인/반려) + 라우트 `/admin/attendance-excuses` + 사이드바 "출석 인정 관리"
   - D5(ⓐ) 반영: 승인 시 매칭 세션에 `공결` 기록 → 분모 유지·분자 제외. `sessions_with_counts` 변경 불필요
   - 검증: `tsc --noEmit`(=프로젝트 lint) 통과. **⚠️ 남은 작업: 원격 DB에 마이그레이션 적용(`supabase db push`)**
2. **M2 — 명단 출석률 스코프 쿼리(D2 ✅)** + 필터별 인원수(총 N명) + **기수 드롭다운 단일화(D3 ✅)**. *데이터 정확성 기반.*
3. **M3 — 커스텀필드 타입 시스템(§5.2)** 핵심5(D6) + 순서변경 UI + 하위옵션 + 일괄 역할변경.
4. **M4 — 명단 분석 대시보드 고도화**(수료율/이탈률/리텐션/휴식복귀전환율/구간분포 히스토그램/개인 streak). PDF p25~29 SQL 참고.
5. **M5 — 합격자 일괄 끌어오기(D1 ✅)**: 합격자 다중선택 → 명단 추가. (기존 자동등록 트리거와 공존)
6. **M6 — 모집 폴리시**: 후기 평점 강화(D7)·선발 프로세스 타임라인·공개 공고 좌/우 레이아웃·FAQ 토글.
7. **보류**: TRM(채용 캘린더), 권한분리(Privacy View) — 기획안도 홀드.
